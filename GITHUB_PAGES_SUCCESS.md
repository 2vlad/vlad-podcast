# ✅ GitHub Pages Auto-Publish - УСПЕШНО НАСТРОЕНО!

## 🎉 Поздравляем! Автоматическая публикация подкаста работает!

### 📡 Ваш подкаст онлайн:
- **RSS Feed:** https://2vlad.github.io/vlad-podcast/rss.xml
- **GitHub Repository:** https://github.com/2vlad/vlad-podcast
- **Статус:** 🟢 РАБОТАЕТ

---

## 🚀 Что было сделано:

### 1. **Настройка репозитория**
- ✅ Создан публичный репозиторий `2vlad/vlad-podcast`
- ✅ Включен GitHub Pages с автоматическим деплоем

### 2. **Структура проекта**
```
youtube-podcast/
├── docs/               # GitHub Pages (публичный контент)
│   ├── rss.xml        # RSS feed (автоматически копируется)
│   ├── media/         # Медиа-файлы (только структура)
│   └── index.md       # Описание подкаста
├── podcast/           # Локальные файлы (не в git)
│   ├── rss.xml        # Исходный RSS
│   └── media/         # Аудио файлы
└── .github/workflows/
    └── deploy-pages.yml  # GitHub Actions для автодеплоя
```

### 3. **Автоматический workflow**
- ✅ GitHub Actions автоматически деплоит `docs/` при push
- ✅ RSS файл автоматически копируется в `docs/` при добавлении эпизода
- ✅ Медиа-файлы хранятся отдельно (не в git)

### 4. **Код интеграции**
- ✅ `utils/github_publisher.py` - автоматический git push
- ✅ `web.py` - интеграция с веб-интерфейсом
- ✅ `regenerate_feed.py` - скрипт для пересоздания feed

---

## 📝 Как использовать:

### Добавить новый эпизод:
1. Откройте http://localhost:5001
2. Вставьте YouTube URL
3. Нажмите "Convert"
4. **Готово!** Эпизод автоматически:
   - Скачается
   - Добавится в RSS
   - Опубликуется на GitHub Pages

### Проверить статус:
```bash
# Проверить RSS feed
curl https://2vlad.github.io/vlad-podcast/rss.xml

# Проверить статус workflow
gh run list --workflow=deploy-pages.yml --limit 3

# Проверить репозиторий
gh repo view 2vlad/vlad-podcast
```

---

## ⚙️ Конфигурация

### Файл `.env`:
```bash
# GitHub Pages URLs
SITE_URL="https://2vlad.github.io/vlad-podcast"
MEDIA_BASE_URL="https://2vlad.github.io/vlad-podcast/media"

# Auto-publish
AUTO_PUBLISH="github"
GITHUB_REPO="2vlad/vlad-podcast"
GITHUB_BRANCH="main"

# Podcast metadata
PODCAST_TITLE="Влад Слушает"
PODCAST_DESCRIPTION="Запаиси для прогулок"
PODCAST_AUTHOR="Влад"
PODCAST_LANGUAGE="ru"
PODCAST_CATEGORY="Leisure"
AUDIO_FORMAT="m4a"
```

---

## 🔧 Технические детали:

### GitHub Actions Workflow:
- **Триггер:** push в `main` branch с изменениями в `docs/`
- **Действие:** Деплой содержимого `docs/` на GitHub Pages
- **Время:** ~30-60 секунд после push

### GitHubPublisher:
1. Копирует `podcast/rss.xml` → `docs/rss.xml`
2. Выполняет `git add docs/`
3. Создает commit с названием эпизода
4. Пушит в `main` branch
5. Автоматически запускается GitHub Actions

### Хранение медиа:
- **Локально:** `podcast/media/*.m4a` (не в git)
- **Ссылки в RSS:** URLs от Mave или внешнего хостинга
- **GitHub Pages:** только RSS feed и метаданные

---

## 📱 Добавление в подкаст-приложения:

### Apple Podcasts:
1. Откройте приложение Podcasts
2. Найдите "Library" > "Add a Show by URL"
3. Вставьте: `https://2vlad.github.io/vlad-podcast/rss.xml`

### Overcast / Pocket Casts:
1. Settings → Add by URL
2. Вставьте RSS URL
3. Подпишитесь

### Любое приложение поддерживающее RSS:
Просто используйте URL: `https://2vlad.github.io/vlad-podcast/rss.xml`

---

## 🎯 Следующие шаги:

1. **Добавьте обложку подкаста** (опционально)
   - Создайте файл `docs/cover.jpg` (1400x1400px)
   - Обновите `.env`: `PODCAST_IMAGE_URL="https://2vlad.github.io/vlad-podcast/cover.jpg"`

2. **Настройте описание** (опционально)
   - Отредактируйте `docs/index.md`
   - Добавьте информацию о подкасте

3. **Тестируйте автопубликацию**
   - Добавьте новый эпизод через веб-интерфейс
   - Проверьте, что он появился в RSS feed
   - Убедитесь, что GitHub Actions выполнился успешно

---

## ⚠️ Важные заметки:

1. **Медиа-файлы не в git:**
   - `.m4a` и `.mp3` исключены из git
   - Используйте внешний хостинг для аудио (Mave, S3, etc.)
   - RSS feed содержит только ссылки на внешние URL

2. **Время обновления:**
   - После добавления эпизода: ~30-60 секунд
   - GitHub Actions может занять 1-2 минуты
   - RSS feed обновляется автоматически

3. **Лимиты GitHub:**
   - GitHub Pages: 1GB storage
   - Bandwidth: 100GB/month  
   - Build time: 10 минут max

---

## 🆘 Troubleshooting:

### RSS feed не обновляется:
```bash
# Проверить статус workflow
gh run list --limit 1

# Посмотреть логи последнего run
gh run view

# Вручную запустить workflow
gh workflow run deploy-pages.yml
```

### Эпизод не опубликовался:
```bash
# Проверить git статус
git status

# Проверить логи сервера
tail -f web_github.log

# Вручную запустить publisher
cd /Users/admin/Dev/youtube-podcast
venv/bin/python -c "
from utils.github_publisher import GitHubPublisher
from config import get_settings
settings = get_settings()
publisher = GitHubPublisher(settings.base_dir)
publisher.publish('Test episode', rss_file=settings.rss_file)
"
```

### Пересоздать RSS feed:
```bash
cd /Users/admin/Dev/youtube-podcast
venv/bin/python regenerate_feed.py
git add docs/rss.xml
git commit -m "Regenerate RSS feed"
git push
```

---

## ✅ Итого:

Автоматическая публикация подкаста на GitHub Pages **полностью работает**! 

Каждый новый эпизод автоматически:
1. ⬇️ Скачивается с YouTube
2. 📝 Добавляется в RSS feed
3. 📤 Публикуется на GitHub Pages
4. 🌐 Становится доступным в подкаст-приложениях

**Наслаждайтесь своим подкастом! 🎧**
