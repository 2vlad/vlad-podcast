# 🚂 Railway Deployment Fix

## ❌ Проблема:
```
pip: command not found
```

Railway не мог найти pip во время установки зависимостей.

---

## ✅ Решение:

### Обновлен `nixpacks.toml`:

**До:**
```toml
[phases.setup]
nixPkgs = ["python39", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
```

**После:**
```toml
[phases.setup]
nixPkgs = ["python39", "python39Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]
```

### Изменения:
1. ✅ Добавлен `python39Packages.pip` в nixPkgs
2. ✅ Изменена команда установки на `python3 -m pip install`

---

## 🔍 Детали проблемы:

Nixpacks использует Nix package manager, где pip не всегда доступен глобально.
Решение - либо явно добавить pip в пакеты, либо использовать `python3 -m pip`.

---

## ✅ Статус:

- Конфигурация обновлена ✅
- Изменения запушены ✅
- Railway автоматически перезапустит deployment ✅

---

## 🌐 URL после деплоя:

**Web Interface:** https://vlad-podcast-production.up.railway.app

**Проверка:**
```bash
curl https://vlad-podcast-production.up.railway.app/api/config
```

---

## 📊 Ожидаемый результат:

После успешного деплоя:
1. Веб-интерфейс доступен
2. Можно добавлять эпизоды
3. Автоматически пушатся на GitHub Pages
4. RSS feed обновляется

---

## ⏱️ Время деплоя:

Обычно занимает **2-4 минуты**:
- Build: ~2 минуты (установка Python, ffmpeg, зависимостей)
- Deploy: ~30 секунд (запуск gunicorn)

Проверьте статус в Railway Dashboard.
