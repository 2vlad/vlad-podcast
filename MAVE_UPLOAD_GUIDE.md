# Mave.digital Upload Guide

## 🎯 Автоматическая загрузка на Mave.digital

После обработки YouTube видео, файлы можно автоматически загружать на Mave.digital hosting.

## 📋 Доступные методы

### 1. Manual (по умолчанию) ✋
**Рекомендуется для начала**

```bash
MAVE_UPLOAD_METHOD="manual"
```

**Как работает:**
- Файлы сохраняются локально в `podcast/media/`
- RSS фид генерируется в `podcast/rss.xml`
- Загрузка на Mave.digital вручную через их веб-интерфейс

**Плюсы:**
- ✅ Нет настройки credentials
- ✅ Полный контроль над процессом
- ✅ Можно проверить файлы перед загрузкой

**Минусы:**
- ❌ Требует ручной загрузки каждого файла

---

### 2. WebDAV 🌐
**Рекомендуется если Mave.digital поддерживает WebDAV**

```bash
MAVE_UPLOAD_METHOD="webdav"
MAVE_WEBDAV_URL="https://cloud.mave.digital/67282/webdav"
MAVE_USERNAME="your_username"
MAVE_PASSWORD="your_password"
```

**Установка:**
```bash
pip install webdavclient3
```

**Плюсы:**
- ✅ Прямая загрузка через HTTP
- ✅ Работает через HTTPS (безопасно)
- ✅ Не требует дополнительных портов
- ✅ Простая настройка

**Минусы:**
- ❌ Нужно уточнить у Mave.digital поддержку WebDAV

---

### 3. FTP 📁
**Классический метод**

```bash
MAVE_UPLOAD_METHOD="ftp"
MAVE_FTP_HOST="ftp.mave.digital"
MAVE_USERNAME="your_ftp_user"
MAVE_PASSWORD="your_ftp_password"
MAVE_FTP_PORT="21"
```

**Встроенная поддержка** (не требует установки)

**Плюсы:**
- ✅ Работает везде где есть FTP
- ✅ Стандартный протокол
- ✅ Встроенная поддержка в Python

**Минусы:**
- ❌ Небезопасный (пароли передаются открытым текстом)
- ❌ Может блокироваться файрволами

---

### 4. SFTP 🔒
**Безопасный FTP через SSH**

```bash
MAVE_UPLOAD_METHOD="sftp"
MAVE_FTP_HOST="sftp.mave.digital"
MAVE_USERNAME="your_ssh_user"
MAVE_PASSWORD="your_ssh_password"
MAVE_FTP_PORT="22"
```

**Установка:**
```bash
pip install paramiko
```

**Плюсы:**
- ✅ Безопасная передача (SSH encryption)
- ✅ Поддержка SSH keys
- ✅ Надежный протокол

**Минусы:**
- ❌ Требует SSH доступ
- ❌ Немного сложнее настройки

---

### 5. rsync 🔄
**Синхронизация файлов**

```bash
MAVE_UPLOAD_METHOD="rsync"
MAVE_FTP_HOST="your-server.com"
MAVE_USERNAME="your_ssh_user"
# Требует SSH key setup (без пароля)
```

**Требования:**
- rsync установлен на сервере
- SSH key authentication настроен

**Плюсы:**
- ✅ Инкрементальная синхронизация (загружает только изменения)
- ✅ Очень быстро для повторных загрузок
- ✅ Надежный

**Минусы:**
- ❌ Требует SSH доступ с keys
- ❌ Сложная настройка

---

## 🚀 Пошаговая настройка

### Шаг 1: Узнайте метод доступа к Mave.digital

Свяжитесь с поддержкой Mave.digital и спросите:

```
Добрый день!

Я использую ваш хостинг для подкаста:
https://cloud.mave.digital/67282

Как я могу автоматически загружать файлы?
Какие протоколы поддерживаются:
- WebDAV?
- FTP/SFTP?
- API?

Нужны credentials для автоматической загрузки.

Спасибо!
```

### Шаг 2: Получите credentials

После ответа от Mave, вы получите:
- Host / URL
- Username
- Password
- Port (если нужен)

### Шаг 3: Настройте .env

Скопируйте нужные настройки в `.env`:

**Для WebDAV:**
```bash
MAVE_UPLOAD_METHOD="webdav"
MAVE_WEBDAV_URL="https://cloud.mave.digital/67282/webdav"
MAVE_USERNAME="vlad_67282"
MAVE_PASSWORD="your_secure_password"
```

**Для SFTP:**
```bash
MAVE_UPLOAD_METHOD="sftp"
MAVE_FTP_HOST="sftp.mave.digital"
MAVE_USERNAME="vlad_67282"
MAVE_PASSWORD="your_secure_password"
MAVE_FTP_PORT="22"
```

### Шаг 4: Установите зависимости (если нужно)

```bash
# Для WebDAV
pip install webdavclient3

# Для SFTP
pip install paramiko
```

### Шаг 5: Тест подключения

```python
from utils.mave_uploader import MaveUploader
from pathlib import Path

# Загрузите настройки из .env
from config import get_settings
settings = get_settings()

# Создайте uploader
credentials = {
    'webdav_url': settings.mave_webdav_url,
    'username': settings.mave_username,
    'password': settings.mave_password,
}

uploader = MaveUploader(
    site_url=settings.site_url,
    upload_method=settings.mave_upload_method,
    credentials=credentials
)

# Тест подключения
if uploader.test_connection():
    print("✅ Подключение успешно!")
else:
    print("❌ Ошибка подключения")
```

### Шаг 6: Запустите веб-сервер

```bash
python web.py
```

Теперь при обработке YouTube видео:
1. Скачается аудио
2. Сгенерируется RSS
3. **Автоматически загрузится на Mave.digital** 🚀

---

## 🔍 Проверка загрузки

После успешной загрузки:

1. Откройте https://cloud.mave.digital/67282/media/
2. Проверьте что файл появился
3. Откройте https://cloud.mave.digital/67282/rss.xml
4. Убедитесь что новый эпизод в фиде

---

## ⚠️ Troubleshooting

### Проблема: "Connection failed"

**Решение:**
- Проверьте credentials (username, password)
- Проверьте URL/Host
- Проверьте firewall/VPN
- Проверьте что порт открыт

### Проблема: "Authentication failed"

**Решение:**
- Неверный username/password
- Возможно требуется 2FA (попробуйте app password)
- Проверьте регистр букв

### Проблема: "Permission denied"

**Решение:**
- У пользователя нет прав на запись
- Попробуйте создать папку вручную
- Свяжитесь с поддержкой Mave

### Проблема: "Module not found: webdavclient3"

**Решение:**
```bash
pip install webdavclient3
```

### Проблема: "Module not found: paramiko"

**Решение:**
```bash
pip install paramiko
```

---

## 🎯 Рекомендации

### Для начинающих
Используйте **Manual** метод:
- Простой старт
- Можно проверить файлы перед загрузкой
- Полный контроль

### Для продакшена
Используйте **WebDAV** или **SFTP**:
- Автоматическая загрузка
- Безопасная передача
- Надежно

### Для экспертов
Используйте **rsync**:
- Максимальная эффективность
- Инкрементальная синхронизация
- Идеально для больших объемов

---

## 📊 Сравнение методов

| Метод   | Безопасность | Скорость | Простота | Авто-загрузка |
|---------|--------------|----------|----------|---------------|
| Manual  | ⭐⭐⭐       | ⭐       | ⭐⭐⭐⭐ | ❌            |
| WebDAV  | ⭐⭐⭐       | ⭐⭐⭐   | ⭐⭐⭐   | ✅            |
| FTP     | ⭐           | ⭐⭐     | ⭐⭐⭐⭐ | ✅            |
| SFTP    | ⭐⭐⭐⭐     | ⭐⭐     | ⭐⭐⭐   | ✅            |
| rsync   | ⭐⭐⭐⭐     | ⭐⭐⭐⭐ | ⭐⭐     | ✅            |

---

## 💡 Советы

1. **Храните пароли в безопасности**
   - Не коммитьте `.env` в git
   - Используйте `.env` (уже в .gitignore)

2. **Регулярно проверяйте загрузки**
   - Откройте Mave.digital и проверьте файлы
   - Проверьте RSS фид

3. **Используйте HTTPS/SFTP когда возможно**
   - Безопаснее передача данных
   - Защита от перехвата

4. **Делайте бэкапы**
   - Локальные файлы сохраняются даже если загрузка не удалась
   - Всегда есть копия в `podcast/media/`

---

## 🆘 Нужна помощь?

1. Проверьте логи: `tail -f web_server.log`
2. Попробуйте test_connection() функцию
3. Свяжитесь с поддержкой Mave.digital
4. Откройте issue в GitHub проекта

---

**Удачи с автоматической загрузкой! 🚀**
