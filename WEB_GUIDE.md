# 🌐 Веб-интерфейс для YouTube to Podcast

Минималистичный веб-интерфейс в стиле Apple для конвертации YouTube в подкаст.

## 🚀 Быстрый старт

### 1. Запуск сервера

```bash
# Активируем виртуальное окружение
source venv/bin/activate

# Запускаем веб-сервер
python web.py
```

### 2. Открываем браузер

Перейдите на: **http://localhost:5000**

### 3. Используем интерфейс

1. ✅ Вставьте ссылку YouTube в поле ввода
2. ✅ Нажмите кнопку со стрелкой (или Enter)
3. ✅ Дождитесь обработки
4. ✅ Файл готов к загрузке на Mave.digital!

## 🎨 Дизайн

Полностью в стиле Apple:
- ⚪️ Черно-белая цветовая схема
- 🎯 Минимум текста, максимум юзабилити
- ✨ Плавные анимации
- 📱 Адаптивный дизайн (работает на телефоне)
- 🌙 Автоматический Dark Mode (если включен в системе)

## 📁 Структура проекта

```
youtube-podcast/
├── web.py              # Flask сервер
├── templates/
│   └── index.html      # HTML интерфейс
└── static/
    ├── style.css       # Apple-style CSS
    └── app.js          # JavaScript логика
```

## 🔧 Как это работает

1. **Frontend (index.html + app.js)**
   - Принимает URL от пользователя
   - Отправляет на backend через API
   - Показывает статус обработки в реальном времени
   - Отображает результат

2. **Backend (web.py)**
   - Flask API сервер
   - Создает фоновые задачи (threading)
   - Использует существующие модули (downloader, rss_manager)
   - Отдает статус через REST API

3. **Workflow**
   ```
   Пользователь вставляет URL
        ↓
   JavaScript отправляет POST /api/process
        ↓
   Backend создает фоновую задачу
        ↓
   Frontend периодически проверяет GET /api/status/:id
        ↓
   Показывает прогресс: "Downloading..." → "Converting..." → "Done!"
        ↓
   Файл готов в podcast/media/
   ```

## 🎯 API Endpoints

### POST /api/process
Запускает обработку URL

**Request:**
```json
{
  "url": "https://www.youtube.com/watch?v=VIDEO_ID"
}
```

**Response:**
```json
{
  "job_id": 1,
  "status": "pending"
}
```

### GET /api/status/:job_id
Проверяет статус задачи

**Response:**
```json
{
  "id": 1,
  "status": "completed",
  "message": "Ready to upload",
  "title": "Video Title",
  "duration": "12:34",
  "file_name": "VIDEO_ID.m4a",
  "file_size": "25.3 MB"
}
```

### GET /api/config
Получает текущую конфигурацию

**Response:**
```json
{
  "podcast_title": "Влад Слушает",
  "audio_format": "m4a",
  "site_url": "https://cloud.mave.digital/67282"
}
```

## 🔐 Безопасность

Текущая версия предназначена для **локального использования**:
- ✅ Запускается на localhost
- ✅ Нет аутентификации (не нужна для локального использования)
- ⚠️ Не рекомендуется открывать в интернет без дополнительной защиты

**Для публичного доступа нужно добавить:**
- Аутентификацию (логин/пароль)
- HTTPS (SSL сертификат)
- Rate limiting (ограничение запросов)
- CORS настройки

## 📱 Мобильная версия

Интерфейс полностью адаптивен:
- Работает на iPhone/Android
- Оптимизирован для сенсорного управления
- Крупные кнопки
- Адаптивные размеры шрифтов

## 🎨 Кастомизация

### Изменить цвета

Отредактируйте `static/style.css`:

```css
/* Основной цвет */
body {
    background: #ffffff;  /* Белый фон */
    color: #1d1d1f;       /* Черный текст */
}

/* Акцентный цвет кнопки */
.btn-primary {
    background: #1d1d1f;  /* Черная кнопка */
}
```

### Изменить иконку

Замените SVG в `templates/index.html` в секции `.icon-logo`

## 🐛 Отладка

### Проблема: "Address already in use"
Порт 5000 занят. Измените порт в `web.py`:
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # Используем 5001
```

### Проблема: Долгая загрузка
Проверьте логи в консоли где запущен `python web.py`

### Проблема: Ошибка конфигурации
Убедитесь что `.env` файл настроен корректно

## 🚀 Продвинутое использование

### Запуск в фоне (production)

```bash
# Используем gunicorn (нужно установить)
pip install gunicorn

# Запускаем с 4 воркерами
gunicorn -w 4 -b 0.0.0.0:5000 web:app
```

### Docker (опционально)

```dockerfile
FROM python:3.9
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "web.py"]
```

## 💡 Советы

1. **Держите терминал открытым** - там будут логи обработки
2. **Не закрывайте браузер** во время обработки
3. **Файлы сохраняются** в `podcast/media/` независимо от веб-интерфейса
4. **Можно использовать оба варианта**: и веб, и CLI

## 🎊 Готово!

Теперь у вас есть красивый минималистичный интерфейс для конвертации YouTube в подкаст!

**Workflow:**
```
1. Открыть http://localhost:5000
2. Вставить ссылку YouTube
3. Нажать Enter
4. Подождать (~1-3 минуты)
5. Файл готов в podcast/media/
6. Загрузить на Mave.digital
7. Наслаждаться подкастом! 🎧
```
